import React from 'react';
import { Link } from 'react-router-dom';

const MiBotonLogin = () => {
    return (
        <Link to="/Formulario">
            <button>Ingresar</button>
        </Link>
    );
};

export default MiBotonLogin;
